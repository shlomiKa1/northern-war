העתקת משתנים שצריך ב env

```bash
cp backend/src/example.env backend/src/.env
```

פקודת הרצה:

```bash
docker-compose up --build
```

---

## בחירת מבנה נתונים

בחרתי ב MongoDB כי יש פה ישויות במערך וגם יש יישות שלא מופיע בכולם (מפקדה).
